# ADMail — Android Project

Generated by **WebToAPK Studio**

## Quick Start

```bash
# Debug build (no signing needed)
./gradlew assembleDebug
# → app/build/outputs/apk/debug/app-debug.apk

# Release build (requires keystore)
./gradlew assembleRelease
# → app/build/outputs/apk/release/app-release.apk
```

## Signing for Play Store

```bash
keytool -genkeypair -v -keystore keystore.jks -keyalg RSA -keysize 2048 \
  -validity 10000 -alias mykey
```

Then fill in the `signingConfigs.release` block in `app/build.gradle`.

## App Configuration
| Setting | Value |
|---------|-------|
| Package | `com.admail1.temp` |
| URL | https://admail1.base44.app |
| Min SDK | API 21 |
| Target SDK | API 34 |
| Orientation | portrait |
| Fullscreen | true |
| Zoom | false |
| Icon | Included in mipmap-* folders + play_store_icon.png |

## CI/CD
A GitHub Actions workflow is included at `.github/workflows/build.yml`.
Push to GitHub → Actions automatically builds and uploads the APK as an artifact.
